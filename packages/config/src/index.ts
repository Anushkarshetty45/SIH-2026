// Shared configuration utilities — populated as needed.

export const DEFAULT_PAGINATION_LIMIT = 20;
export const MAX_PAGINATION_LIMIT = 100;

/** Default referral response timeout in minutes (matches SRS). */
export const REFERRAL_TIMEOUT_MINUTES = 30;
